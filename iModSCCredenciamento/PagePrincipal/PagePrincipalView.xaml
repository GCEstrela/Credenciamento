<UserControl  x:Class="iModSCCredenciamento.PagePrincipal.PagePrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:iModSCCredenciamento.Views"
             xmlns:viewmodels="clr-namespace:iModSCCredenciamento.ViewModels"
             xmlns:local="clr-namespace:iModSCCredenciamento.PagePrincipal"
             mc:Ignorable="d" 
             d:DesignHeight="300" MinWidth="640" >
    <!--<UserControl.Resources>
        <DataTemplate x:Name="EmpresaViewTemplate" DataType="{x:Type viewmodels:EmpresaViewModel}">
            <views:EmpresaView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="ColaboradorViewTemplate" DataType="{x:Type viewmodels:ColaboradorViewModel}">
            <views:ColaboradorView DataContext="{Binding}"/>
        </DataTemplate>
    </UserControl.Resources>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition x:Name="Row2" Height="89*"/>
        </Grid.RowDefinitions>

        <local:MenuPrincipal  Height="60" VerticalAlignment="Top" Margin="0,20,0,0" ButtonClick="OnButtonClick"/>
        <ContentControl Content="{Binding}" Grid.Row="1"/>
    </Grid>

</UserControl>

