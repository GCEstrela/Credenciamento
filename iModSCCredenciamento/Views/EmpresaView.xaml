<UserControl x:Class="iModSCCredenciamento.Views.EmpresaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:iModSCCredenciamento.Views"
             xmlns:i="clr-namespace:iModSCCredenciamento.Funcoes"
             mc:Ignorable="d"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ControlTemplate x:Key="validationErrorTemplate">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                    <Grid Width="12" Height="12">
                        <Ellipse Width="12" Height="12" Fill="Red" HorizontalAlignment="Center"
                                 VerticalAlignment="Center" />

                        <TextBlock Foreground="White" FontWeight="Heavy" FontSize="8" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" TextAlignment="Center"
                                   ToolTip="{Binding ElementName=ErrorAdorner, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}">
                            X
                        </TextBlock>
                    </Grid>
                    <TextBlock Foreground="Red" Margin="2,0,0,0"
                               Text="{Binding ElementName=ErrorAdorner, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}" />
                </StackPanel>
                <AdornedElementPlaceholder x:Name="ErrorAdorner" />
            </DockPanel>
        </ControlTemplate>

        <Style x:Key="MenuStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{x:Null}" />
            <Setter Property="BorderBrush" Value="{x:Null}" />
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="0" Background="{TemplateBinding Background}">
                            <Grid>
                                <ContentPresenter x:Name="MyContentPresenter" Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="0,0,0,0" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <i:FormateTel x:Key="FormateTel" />
    </UserControl.Resources>
    <Grid Margin="10" ShowGridLines="True">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>


        <StackPanel x:Name="ListaEmpresas_sp" Margin="10" Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
            <ListView x:Name="ListaEmpresas_lv" ItemsSource="{Binding Empresas, Mode=TwoWay}"
                      SelectedIndex="{Binding Path=SelectedIndex, Mode=TwoWay}"
                      SelectedItem="{Binding Path = EmpresaSelecionada, Mode=TwoWay}"
                      SelectionChanged="ListaEmpresas_lv_SelectionChanged" HorizontalAlignment="Left" Width="800">
                <ListView.View>
                    <GridView FrameworkElement.FlowDirection="RightToLeft">
                        <GridViewColumn>
                            <!--<GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image
                                        Source="{Binding Pendente,Converter={StaticResource ConvertBoolToImage}
                                            ,UpdateSourceTrigger=Explicit, Mode=TwoWay}"
                                        Width="16" Height="16" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>-->
                        </GridViewColumn>
                        <GridViewColumn Width="80" Header="Código" DisplayMemberBinding="{Binding EmpresaID}" />
                        <GridViewColumn Width="150" Header="CNPJ" DisplayMemberBinding="{Binding CNPJ}" />
                        <GridViewColumn Width="200" Header="Nome" DisplayMemberBinding="{Binding Apelido}" />
                        <GridViewColumn Width="450" Header="Razão Social" DisplayMemberBinding="{Binding Nome}" />
                        <GridViewColumn Width="200" Header="Telefone 1" DisplayMemberBinding="{Binding Telefone1}" />
                        <GridViewColumn Width="200" Header="Celular 1" DisplayMemberBinding="{Binding Celular1}" />
                        <GridViewColumn Width="200" Header="E-Mail 1" DisplayMemberBinding="{Binding Email1}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel x:Name="Botoes_Principais_sp" Orientation="Vertical" HorizontalAlignment="Left" Margin="5,0,5,0" Grid.Row="0">
                <Button x:Name="Pesquisar_bt" Content="Pesquisar" Width="100" Margin="5" />
                <Button x:Name="Adicionar_bt" Content="Adicionar" Width="100" Margin="5" />
                <Button x:Name="Editar_bt" Content="Editar" Width="100" Margin="5" />
                <Button x:Name="Excluir_bt" Content="Excluir" Width="100" Margin="5" />
                <Button x:Name="Salva_bt" Content="Salvar" Width="100" Margin="5" />
                <Button x:Name="Cancelar_bt" Content="Cancelar" Width="100" Margin="5" />
            </StackPanel>
        </StackPanel>
        
        <!--#region Menu-->

        <!--#region TextBlock -->
        <Border CornerRadius="3" BorderThickness="1" Grid.Row="1" BorderBrush="White" Margin="10,5,10,5" Height="25"   >
            <TextBlock Text="{Binding SelectedItem.Nome, ElementName=ListaEmpresas_lv}" TextWrapping="Wrap"  HorizontalAlignment="Stretch"  Margin="10,2,10,2" FontSize="14" VerticalAlignment="Center"/>
        </Border>
        <!--#endregion-->


    </Grid>
</UserControl>